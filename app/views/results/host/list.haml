- @page_title = "Browse Hosts"
%h1.page-title= @page_title
%table.list
  %tr
    %th Name
    %th Operations
  - @records.each do |h|
    %tr
      %td= h(h.name)
      %td
        %ul.actions
          %li= link_for(h, :label => 'Show')
= render(:partial => '/common/pagination', :locals => {:record_pages => @record_pages})
